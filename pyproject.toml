[project]
name = "rwanda_sarimax"
version = "0.1.0"
description = "Rwanda SARIMAX forecasting model for CHAP"
requires-python = ">=3.11"
dependencies = [
    "pandas",
    "statsmodels",
    "joblib",
]

[tool.hatch.build.targets.wheel]
packages = []
